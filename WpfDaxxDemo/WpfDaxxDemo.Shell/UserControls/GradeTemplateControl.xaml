<UserControl x:Class="WpfDaxxDemo.Shell.UserControls.GradeTemplateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="parent"
             d:DesignHeight="50" d:DesignWidth="100">
    <Grid DataContext="{Binding ElementName=parent}">
        <StackPanel Orientation="Vertical">
            <TextBlock Grid.Row="0" Text="{Binding Path=Label}" Style="{StaticResource RedText}"
                 Width="50"
                 Margin="3"
                 VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Text="{Binding Path=Text}" FontFamily="Arial"
               Width="50"
               Margin="3"/>
        </StackPanel>
    </Grid>
</UserControl>
